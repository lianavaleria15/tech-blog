const signIn = (req, res) => {
  res.send("signin");
};

const login = (req, res) => {
  res.send("signin");
};

const logout = (req, res) => {
  res.send("signin");
};

module.exports = { signIn, login, logout };
